{
    "http-basic": {
        "packages.vaimo.com": {
            "username": "${COMPOSER_VAIMO_USERNAME}",
            "password": "${COMPOSER_VAIMO_PASSWORD}"
        },
        "api.vaimo.com": {
            "username": "${VAIMO_API_USERNAME}",
            "password": "${VAIMO_API_PASSWORD}"
        },
        "repo.magento.com": {
            "username": "${COMPOSER_MAGENTO_USERNAME}",
            "password": "${COMPOSER_MAGENTO_PASSWORD}"
        }
    }
}